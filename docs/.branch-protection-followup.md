# Repository administration follow-up

The 0.13 Legacy Final regression workflow is proven on Windows/Linux and Python 3.10/3.12.

GitHub repository settings should require the relevant regression checks on `main` via branch protection or a ruleset. This file is intentionally only a tracking note: the connected GitHub actions used during the 0.13 implementation can read protection/ruleset state but do not expose a mutation for enabling it.

Remove this note once required-check enforcement is configured in repository settings.
